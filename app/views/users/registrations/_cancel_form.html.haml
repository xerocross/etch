%h3 Cancel Account
.narrow-form
  %p.alert.alert-warning
    Unhappy?  This action will stop any auto-renew charges.  Your account will still be fully functional until the end of your current subscription period (plus a very short grace period).

  = button_to "Cancel My Subscription", registration_path(resource_name),data: { confirm: "Are you sure?" }, method: :delete,class:'btn btn-danger'